      <div>
      
      <% @mentions.each do |mentions| %>
        <% if session[:user_id] %>
          <div class="element">
            <img src="<%= mentions.user.profile_image_url =%>" />
            <h2><%= mentions.user.name =%> (<%= "@"+mentions.user.screen_name =%>)</h2>
            <%= mentions.text =%>
          </div>        
          <% else %>
          <p> Sign in first </p>
          <% end %>
      <% end %>
      </ul>
    </div>
<!--    <form>
      <div class="control-group">
      </div>
      <input name="tweet" type="text" value="Tweet this">
      <input type="submit"/>
    </form> -->
