<!DOCTYPE html>
<html>
<head>
  <title>SimpleTweet</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<!-- <%= link_to "Sign in with Twitter", "/auth/twitter/" %> -->
    <nav>
      <h1><%= link_to "Simple Tweet", "#{root_url}" %></h1>        
<% if session[:user_id] %>
  <ul> 
    <li><%= link_to "Timeline", "#{root_url}tweets/timeline" %></li>
    <li><%= link_to "Mentions", "#{root_url}tweets/mentions" %></li>
    <li><%= link_to "My Tweets", "#{root_url}tweets/mytweets" %></li>
    <li><%= link_to "New Tweet", "#{root_url}tweets/newtweet" %></li> 	
    <li>Signed in as <%= session[:user_id] =%> (<%= link_to "Sign out", "#{root_url}signout" %>)</li>
	</ul>
<% else %> 
		<%= link_to "Sign in with Twitter", "#{root_url}auth/twitter/" %>
<% end %>
	</nav>
      	
<%= yield %>

</body>
</html>
